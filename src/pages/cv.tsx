import { NextPage } from "next";

const CV: NextPage = () => {
    return(
        <article className="prose lg:prose-xl">
            <h1>What is the Q?</h1>
        </article>
    );
};

export default CV;
